{% extends 'base.html.twig' %}

{% block title %}Lecturer{% endblock %}

{% block body %}
    <h1>Lecturer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lecturer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lecturer.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lecturer_index') }}">back to list</a>

    <a href="{{ path('app_lecturer_edit', {'id': lecturer.id}) }}">edit</a>

    {{ include('lecturer/_delete_form.html.twig') }}
{% endblock %}
