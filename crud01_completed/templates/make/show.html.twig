{% extends 'base.html.twig' %}

{% block title %}Make{% endblock %}

{% block body %}
    <h1>Make</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ make.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ make.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('make_index') }}">back to list</a>

    <a href="{{ path('make_edit', {'id': make.id}) }}">edit</a>

    {{ include('make/_delete_form.html.twig') }}
{% endblock %}
